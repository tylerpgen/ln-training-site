import { Typography, Container } from "@mui/material";

const Products = () => {
  return (
    <Container sx={{ display: "flex", m: "auto", justifyContent: "center" }}>
      <Typography sx={{ fontSize: "10rem" }}>Coming Soon...</Typography>
    </Container>
  );
};

export default Products;
